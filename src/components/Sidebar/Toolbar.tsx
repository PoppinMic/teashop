import React from 'react';

const Toolbar: React.SFC<any> = () => (
  <div className="toolbar">
    <span>Account</span>
    <span>Search</span>
    <span>Cart</span>
  </div>
);

export default Toolbar;
